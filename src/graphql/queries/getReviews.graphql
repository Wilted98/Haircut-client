query getReviews($options: GetReviewsOptions!) {
  getReviews(options: $options) {
    id
    comment
    createdAt
    hairstylist_rating
    postedBy {
      id
      name
    }
    user {
      id
      name
    }
  }
}
